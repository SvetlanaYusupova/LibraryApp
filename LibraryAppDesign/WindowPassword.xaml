<Window x:Class="LibraryAppDesign.WindowPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryAppDesign"
        mc:Ignorable="d"
        Title="WindowPassword" Height="150" Width="600">
    <Grid Margin="10" Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock VerticalAlignment="Center">Введите пароль:</TextBlock>
        <TextBox Name="textBoxPassword" VerticalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="2"></TextBox>

        <Button Name="buttonCheck" Grid.Row="2" Click="buttonCheck_Click">Ввести пароль</Button>
        <Button Name="buttonReturn" Grid.Row="2" Grid.Column="1" Click="buttonReturn_Click">Вернуться назад</Button>
    </Grid>
</Window>
